nstalled an offline terminal-based dictionary for use during exams (run using dict: e.g. dict "prime number")
Installed pyenv and pypi for use with the Python projects in the updated curriculum
Installed ollama (which lets you download and use LLM models directly on the cluster computers - models are downloaded to goinfre)
Installed the Vulkan SDK to /opt/vulkan (load it using source /opt/vulkan/latest/setup-env.sh)
Set up a proxy server for pip to increase download speed for pip packages (similar to what we already had for Docker images and apt)